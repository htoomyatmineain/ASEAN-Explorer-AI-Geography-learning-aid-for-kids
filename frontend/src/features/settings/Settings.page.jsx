// Placeholder — reached from the "Setting" nav item. Real settings UI
// (sound, language, reset progress, etc.) comes later.
function SettingsPage() {
  return (
    <div className="flex min-h-[60vh] items-center justify-center">
      <h1 className="text-3xl font-extrabold text-sky-600">Settings</h1>
    </div>
  );
}

export default SettingsPage;
