// Reached by tapping "Start" on the entrance page. Serves as the main menu
// hub — the Layout nav (Learning / Progress / Exit) is the entire page, so
// this renders nothing of its own (an extra min-height div here would make
// the page taller than the viewport and force a scrollbar).
function MainMenuPage() {
  return null;
}

export default MainMenuPage;
