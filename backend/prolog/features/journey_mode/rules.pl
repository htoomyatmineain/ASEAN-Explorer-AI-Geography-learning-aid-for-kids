% backend/prolog/features/journey_mode/rules.pl
% Feature 2 — Travel Through Each Country & Level Up (Journey Mode)
% NOT YET BUILT — see "What's Still Open" in docs/04-asean-explorer-features.md.
%
% Needed predicates, once designed:
%   level/2               — child's current level
%   unlocked_country/2    — which countries are available to play
%   checkpoint_passed/2   — which country-checkpoints are cleared
%
% This file is a placeholder so backend/prolog/kb.pl has something to load and
% the route below can return a clear "not implemented" response instead of a
% 404 the frontend can't distinguish from a real missing-country error.
